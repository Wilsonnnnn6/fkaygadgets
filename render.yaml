services:
  - type: web
    name: fkay-gadget-store
    runtime: node
    plan: free
    buildCommand: ""
    startCommand: npm start
    autoDeploy: true
    envVars:
      - key: NODE_VERSION
        value: 20
